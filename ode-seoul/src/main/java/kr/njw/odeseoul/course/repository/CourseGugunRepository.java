package kr.njw.odeseoul.course.repository;

import kr.njw.odeseoul.course.entity.CourseGugun;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CourseGugunRepository extends JpaRepository<CourseGugun, Long> {
}
